#include <stdio.h>
int main(){
    int num;
    printf("enter a  number");
    scanf("%d",&num);
    if(num>=0 && num<=9)
    {
        printf("single digit");
    }
    else
    {
        printf("not a single digit\n");
    }
    return(0);
}
